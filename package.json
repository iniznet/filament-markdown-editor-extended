{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "node bin/build.js --dev",
        "build": "node bin/build.js",
        "dev:bun": "bun bin/build.js --dev",
        "build:bun": "bun bin/build.js"
    },
    "devDependencies": {
        "esbuild": "^0.19.2",
        "prettier": "^2.7.1"
    }
}
